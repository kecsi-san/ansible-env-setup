# minimal set of developer and/or devops components a base system should have

Kind of start point to setup a dev or devops env.
So it wont try to concentrate on real minimalist env but a good start point instead.
Start point in a way to have all necessary tools and apps.

## install base OS specific packages

currently only Debian supported.
just a list of pkgs we add in in 3 categories.

## install base brew packages

prerequesite here to have (linux)brew installed earlier.
another list of packages I prefer to install and keep up to date using brew.
the reason to use brew is to have more frequent updates (instead of os packages)

## Tool Management

This role spans **Tier 1** (APT packages) and **Tier 2** (Homebrew packages) of the tool management strategy. See [Tool Management Philosophy](../../README.md#tool-management-philosophy).
