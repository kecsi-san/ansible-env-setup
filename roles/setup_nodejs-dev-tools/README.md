# setup_nodejs-dev-tools

**Placeholder — not yet implemented.**

Intended to install Node.js development tooling: runtime, package managers, and common CLI tools.

Candidates: Node.js (via nvm or brew), npm, pnpm, yarn (see setup_apt_repos), npx, tsx, eslint, prettier.
