---
- name: Configure prerequisite settings
  hosts: local
  become: true
  remote_user: "{{ ansible_ssh_user }}"
  roles:
    - role: configure_ssh
      become: true
      tags:
        - ssh
    - role: configure_sudo
      become: true
      tags:
        - sudo
